# PayPort Questionnaire Telegram Bot

Telegram бот для сбора анкет от payment-провайдеров.

## Возможности

- 👑 **Администратор**: управление операторами и редактирование вопросов
- 👤 **Оператор**: создание invite-ссылок, получение заполненных анкет
- 📋 **Респондент**: заполнение анкеты по ссылке-приглашению
- 📄 **Автоматическая генерация .docx** с ответами

## Установка

### 1. Создать бота в Telegram

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Введите имя бота (например: `PayPort Questionnaire Bot`)
4. Введите username бота (например: `payport_questionnaire_bot`)
5. Скопируйте полученный **Bot Token**

### 2. Установить зависимости

```bash
cd /Users/pavel/PROGECTS/chat_bot_payport

# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

### 3. Настроить переменные окружения

```bash
# Создать файл .env
cat > .env << EOF
BOT_TOKEN=your_bot_token_here
FIRST_ADMIN_USERNAME=AlbuRx
EOF
```

**Важно:** Замените `your_bot_token_here` на реальный токен от BotFather!

### 4. Запустить бота

```bash
python run.py
```

## Использование

### Для администратора (@AlbuRx)

1. Напишите `/start` боту
2. Используйте меню для:
   - ➕ Добавить оператора (по @username)
   - ➖ Удалить оператора
   - 📝 Редактировать вопросы

### Для оператора

1. После добавления администратором напишите `/start` боту
2. Используйте меню для:
   - 🔗 Создать ссылку-приглашение
   - 📋 Посмотреть свои приглашения

3. Отправьте ссылку респонденту или в группу
4. Получите .docx файл после заполнения анкеты

### Для респондента

1. Перейдите по полученной ссылке
2. Отвечайте на вопросы текстовыми сообщениями
3. После завершения оператор получит документ с вашими ответами

## Структура проекта

```
chat_bot_payport/
├── bot/
│   ├── __init__.py
│   ├── main.py              # Точка входа
│   ├── config.py            # Конфигурация
│   ├── database.py          # Работа с БД
│   ├── document_generator.py # Генерация .docx
│   ├── keyboards.py         # Клавиатуры
│   ├── states.py            # FSM состояния
│   └── handlers/
│       ├── __init__.py
│       ├── common.py        # Общие команды
│       ├── admin.py         # Команды админа
│       ├── operator.py      # Команды оператора
│       └── questionnaire.py # Анкетирование
├── data/
│   └── questions.json       # Вопросы анкеты
├── generated_docs/          # Сгенерированные документы
├── requirements.txt
├── run.py
└── README.md
```

## Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Начало работы / авторизация |
| `/menu` | Показать меню |
| `/cancel` | Отменить текущую операцию |
| `/help` | Справка |

## База данных

SQLite (`bot_database.db`), создается автоматически.

Таблицы:
- `users` - операторы и админы
- `invites` - ссылки-приглашения
- `questionnaires` - заполненные анкеты
- `questions` - вопросы анкеты

## Редактирование вопросов

Вопросы хранятся в БД и загружаются из `data/questions.json` при первом запуске.

Админ может редактировать вопросы через меню бота.

## TODO

- [ ] Добавить возможность добавления новых вопросов через бота
- [ ] Экспорт статистики по анкетам
- [ ] Уведомления о новых респондентах

